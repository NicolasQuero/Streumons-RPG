/*#ifndef ANIMATION_H_INCLUDED
#define ANIMATION_H_INCLUDED

#include <vector>
#include <unistd.h>
#include "Frame.h"


struct Animation {
private:
    int nbFrames;
    vector<int> timePerFrame;
    vector<Frame> *frames;
public:
    Animation(int _nbFrames, vector<int> _timePerFrame, vector<Frame> *_frames); // timePerFrame must be in microseconds

};


#endif // ANIMATION_H_INCLUDED
*/
